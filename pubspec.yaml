name: theme_bloc
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.3.1 <4.0.0'
dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  flutter_bloc: ^8.1.4
  equatable: ^2.0.5
  hydrated_bloc: ^9.1.4
  path_provider: ^2.1.2
  theme_manager:
    git: https://github.com/ilipovcev/theme_manager

dev_dependencies:
  flutter_test:
    sdk: flutter

  dart_code_linter:
    git:
      url: https://github.com/The-Redhat/dart-code-linter.git
      ref: fix_analyzer_6.0_errors
  lint: ^2.3.0

flutter:

  uses-material-design: true